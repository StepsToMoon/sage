# Keybinding Extension

This extension exists solely to provide a keybind that
installs another extension while skipping the
"Do you trust this publisher" dialog.

We can't use an `extension.js` directly in here because
the CSP for the github.dev page blocks it.
